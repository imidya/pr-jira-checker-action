{
  "name": "pr-jira-checker-action",
  "version": "1.0.0",
  "description": "GitHub Action to scan PR commits for Jira references and update PR description",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist",
    "test": "jest"
  },
  "keywords": [
    "github",
    "action",
    "jira",
    "pull-request"
  ],
  "author": "Sam Huang",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1",
    "jest": "^29.7.0"
  }
}
